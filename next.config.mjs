/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "cdn.inappstory.ru",
            "media.dodostatic.net",
            "cdn.dodostatic.net",
        ],
    },
};

export default nextConfig;
